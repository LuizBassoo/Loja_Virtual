Para conectar ao banco, criar um arquivo '.env' na raiz com os seguintes pares:
Substitua 'suaSenha' pela senha de conexão do banco.
    DB_HOST=localhost
    DB_PORT=5432
    DB_DATABASE=trabalho_1
    DB_USERNAME=postgres
    DB_PASSWORD='suaSenha'